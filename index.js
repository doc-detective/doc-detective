const {
  runTests,
  getRunner,
  detectTests,
  detectAndResolveTests,
  resolveTests,
  readFile,
  resolvePaths,
} = require("./src/core");

module.exports = {
  runTests,
  getRunner,
  detectTests,
  detectAndResolveTests,
  resolveTests,
  readFile,
  resolvePaths,
};
