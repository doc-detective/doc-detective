{
  "specId": "background-processes",
  "description": "Exercises long-running background processes end-to-end: a `background` object ({ name, waitUntil }) on runCode/runShell with each waitUntil condition (port, stdio, httpGet, delayMs), multiple conditions combined, the no-waitUntil form (ready as soon as spawned), closeSurface (by name), the missing-process no-op, and the run-end auto-sweep (a process left running with no closeSurface). All steps are non-driver (Node only), so they run and PASS on every platform; each test uses a distinct port and process name to stay safe under concurrentRunners.",
  "tests": [
    {
      "testId": "bg-runcode-port",
      "description": "runCode background server, port condition, stopped via closeSurface.",
      "steps": [
        {
          "runCode": {
            "language": "javascript",
            "code": "require('http').createServer((q,r)=>r.end('ok')).listen(8201,'127.0.0.1');",
            "background": {
              "name": "bg-port",
              "waitUntil": {
                "port": 8201
              }
            },
            "timeout": 15000
          }
        },
        {
          "closeSurface": "bg-port"
        }
      ]
    },
    {
      "testId": "bg-runcode-stdio",
      "description": "runCode background process, stdio condition (matches stdout or stderr), stopped via closeSurface.",
      "steps": [
        {
          "runCode": {
            "language": "javascript",
            "code": "console.log('DD_BG_READY'); setInterval(() => {}, 1000000);",
            "background": {
              "name": "bg-stdio",
              "waitUntil": {
                "stdio": "DD_BG_READY"
              }
            },
            "timeout": 15000
          }
        },
        {
          "closeSurface": "bg-stdio"
        }
      ]
    },
    {
      "testId": "bg-runcode-httpget",
      "description": "runCode background server, httpGet condition, stopped via closeSurface.",
      "steps": [
        {
          "runCode": {
            "language": "javascript",
            "code": "require('http').createServer((q,r)=>r.end('ok')).listen(8202,'127.0.0.1');",
            "background": {
              "name": "bg-http",
              "waitUntil": {
                "httpGet": "http://127.0.0.1:8202"
              }
            },
            "timeout": 15000
          }
        },
        {
          "closeSurface": "bg-http"
        }
      ]
    },
    {
      "testId": "bg-runcode-delay",
      "description": "runCode background process, fixed-delay condition, stopped via closeSurface.",
      "steps": [
        {
          "runCode": {
            "language": "javascript",
            "code": "setInterval(() => {}, 1000000);",
            "background": {
              "name": "bg-delay",
              "waitUntil": {
                "delayMs": 300
              }
            },
            "timeout": 15000
          }
        },
        {
          "closeSurface": "bg-delay"
        }
      ]
    },
    {
      "testId": "bg-runcode-combined",
      "description": "runCode background server with multiple waitUntil conditions combined (port AND stdio AND delayMs) — all must pass.",
      "steps": [
        {
          "runCode": {
            "language": "javascript",
            "code": "require('http').createServer((q,r)=>r.end('ok')).listen(8205,'127.0.0.1'); console.log('DD_COMBINED_READY');",
            "background": {
              "name": "bg-combined",
              "waitUntil": {
                "port": 8205,
                "stdio": "/DD_COMBINED_READY/",
                "delayMs": 100
              }
            },
            "timeout": 15000
          }
        },
        {
          "closeSurface": "bg-combined"
        }
      ]
    },
    {
      "testId": "bg-runcode-noprobe",
      "description": "Background process with no waitUntil — ready as soon as it is spawned; stopped via closeSurface.",
      "steps": [
        {
          "runCode": {
            "language": "javascript",
            "code": "setInterval(() => {}, 1000000);",
            "background": {
              "name": "bg-noprobe"
            },
            "timeout": 15000
          }
        },
        {
          "closeSurface": "bg-noprobe"
        }
      ]
    },
    {
      "testId": "bg-runshell-port",
      "description": "runShell background server (node bg-server.js), port condition, stopped via closeSurface.",
      "steps": [
        {
          "runShell": {
            "command": "node",
            "args": [
              "test/core-artifacts/bg-server.js",
              "8203"
            ],
            "background": {
              "name": "bg-shell",
              "waitUntil": {
                "port": 8203
              }
            },
            "timeout": 15000
          }
        },
        {
          "closeSurface": "bg-shell"
        }
      ]
    },
    {
      "testId": "bg-autosweep",
      "description": "Background process started with no closeSurface — the run-end sweep must tear it down. The start step PASSES; cleanup is implicit.",
      "steps": [
        {
          "runCode": {
            "language": "javascript",
            "code": "require('http').createServer((q,r)=>r.end('ok')).listen(8204,'127.0.0.1');",
            "background": {
              "name": "bg-autosweep",
              "waitUntil": {
                "port": 8204
              }
            },
            "timeout": 15000
          }
        }
      ]
    },
    {
      "testId": "bg-stopprocess-missing",
      "description": "closeSurface on a name that was never started is a no-op that PASSES.",
      "steps": [
        {
          "closeSurface": "bg-never-started"
        }
      ]
    }
  ]
}
