{
  "specId": "ordering-cleanup-target",
  "description": "Cleanup spec referenced by cleanup-after.spec.json's `after`. Only the first test's steps are used there. Note: the combined core scanner also picks this file up and runs it as a standalone spec (the echo step passes) — that dual role is intentional, so keep its steps self-contained and passing.",
  "tests": [
    {
      "steps": [
        {
          "runShell": "echo ordering-cleanup"
        }
      ]
    }
  ]
}
