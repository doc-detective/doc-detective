import App from './index.js';

// This default export determines where your story goes in the story list.
export default {
  title: 'Doc Detective/App',
  component: App,
  // args at the component level for all stories.
  args: {
  }
};

export const Default = {}