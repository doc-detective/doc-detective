{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "runBrowserScript",
  "description": "Execute arbitrary JavaScript in the browser page context. Runs via the WebDriver `executeScript` endpoint, so it has access to the page's `document`, `window`, and DOM. Doc Detective captures the script's return value in the step's `outputs.result`. Distinct from `runCode`, which runs Node/Python/bash on the host machine.",
  "anyOf": [
    {
      "$ref": "#/x_dd_defs/d7cb49631389c"
    },
    {
      "$ref": "#/x_dd_defs/decc32cd319af"
    }
  ],
  "components": {
    "schemas": {
      "string": {
        "$ref": "#/x_dd_defs/d7cb49631389c"
      },
      "object": {
        "$ref": "#/x_dd_defs/decc32cd319af"
      }
    }
  },
  "examples": [
    "return document.title;",
    {
      "script": "return document.title;"
    },
    {
      "script": "return arguments[0] + arguments[1];",
      "args": [
        "foo",
        "bar"
      ]
    },
    {
      "script": "return window.location.href;",
      "output": "/^https?:\\/\\//"
    },
    {
      "script": "return document.querySelectorAll('a').length;",
      "output": "5"
    },
    {
      "script": "return JSON.stringify({ ready: document.readyState });",
      "path": "ready-state.json",
      "directory": "output",
      "maxVariation": 0.1,
      "overwrite": "aboveVariation"
    },
    {
      "script": "localStorage.setItem('token', arguments[0]);",
      "args": [
        "$AUTH_TOKEN"
      ]
    }
  ],
  "x_dd_defs": {
    "d7cb49631389c": {
      "title": "Run browser script (simple)",
      "description": "JavaScript to evaluate in the browser page context. Supports `return` to capture a value into `outputs.result`.",
      "type": "string",
      "transform": [
        "trim"
      ]
    },
    "d81d8f452f557": {
      "title": "Surface (by browser engine)",
      "type": "string",
      "enum": [
        "chrome",
        "firefox",
        "safari",
        "webkit",
        "edge"
      ],
      "transform": [
        "trim"
      ],
      "description": "Browser engine keyword. Targets that browser. Steps that can only ever act on a browser (not a background process) restrict the bare-string form to this enum, so a process name here is rejected at validation time instead of failing at runtime."
    },
    "d23078c31e9af": {
      "type": "string",
      "enum": [
        "chrome",
        "firefox",
        "safari",
        "webkit",
        "edge"
      ],
      "description": "Browser engine. Selects the browser surface with that engine (or the one named by `name`). A goTo step opens the browser if it isn't open yet — you can also open one explicitly with `startSurface`; other steps require it to already be open."
    },
    "d602d0d67985b": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "transform": [
        "trim"
      ],
      "description": "Name of the browser surface. Defaults to the engine name (the context's default browser registers under its engine). Assign distinct names to drive multiple browsers at once, including several of the same engine."
    },
    "d34d60ebb0a88": {
      "title": "By index",
      "type": "integer",
      "description": "Index in creation order. Negative counts from the end; `-1` is the newest."
    },
    "dcef07241c15a": {
      "title": "By name",
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "transform": [
        "trim"
      ],
      "description": "Name assigned when the window/tab was opened (goTo `newTab`/`newWindow`). The integer branch is listed first because Ajv validates with coerceTypes — string-first would coerce integer indexes into name strings."
    },
    "db83479a87432": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "transform": [
        "trim"
      ],
      "description": "Name assigned when the window/tab was opened."
    },
    "d477cb6dfff92": {
      "name": {
        "$ref": "#/x_dd_defs/db83479a87432"
      },
      "index": {
        "type": "integer",
        "description": "Index in creation order. Negative counts from the end."
      },
      "title": {
        "type": "string",
        "minLength": 1,
        "description": "Page title to match. Substring, or /regex/."
      },
      "url": {
        "type": "string",
        "minLength": 1,
        "description": "Page URL to match. Substring, or /regex/."
      }
    },
    "d9e0c9a379818": {
      "title": "By criteria",
      "type": "object",
      "additionalProperties": false,
      "minProperties": 1,
      "properties": {
        "$ref": "#/x_dd_defs/d477cb6dfff92"
      }
    },
    "d75f5822b9926": {
      "description": "Which window to act on. Omit to use the active window.",
      "title": "Window/tab selector",
      "anyOf": [
        {
          "$ref": "#/x_dd_defs/d34d60ebb0a88"
        },
        {
          "$ref": "#/x_dd_defs/dcef07241c15a"
        },
        {
          "$ref": "#/x_dd_defs/d9e0c9a379818"
        }
      ]
    },
    "df3616f3b9188": {
      "description": "Which tab to act on. Omit to use the active tab. Without `window`, the selector searches every tab in creation order — including tabs the page opened itself.",
      "title": "Window/tab selector",
      "anyOf": [
        {
          "$ref": "#/x_dd_defs/d34d60ebb0a88"
        },
        {
          "$ref": "#/x_dd_defs/dcef07241c15a"
        },
        {
          "$ref": "#/x_dd_defs/d9e0c9a379818"
        }
      ]
    },
    "d231ddad27946": {
      "browser": {
        "$ref": "#/x_dd_defs/d23078c31e9af"
      },
      "name": {
        "$ref": "#/x_dd_defs/d602d0d67985b"
      },
      "window": {
        "$ref": "#/x_dd_defs/d75f5822b9926"
      },
      "tab": {
        "$ref": "#/x_dd_defs/df3616f3b9188"
      }
    },
    "df8c05c280185": {
      "title": "Browser surface",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "browser"
      ],
      "properties": {
        "$ref": "#/x_dd_defs/d231ddad27946"
      }
    },
    "d56daef404d20": {
      "description": "The browser window/tab the script runs in. Omit to run in the active tab. The targeted tab stays focused afterward.",
      "anyOf": [
        {
          "$ref": "#/x_dd_defs/d81d8f452f557"
        },
        {
          "$ref": "#/x_dd_defs/df8c05c280185"
        }
      ]
    },
    "d99cdd6dfd51e": {
      "type": "string",
      "description": "JavaScript to evaluate in the browser page context. Supports `return` to capture a value into `outputs.result`. The script reads arguments supplied in `args` through the `arguments` object (`arguments[0]`, `arguments[1]`, and so on)."
    },
    "d7a61b0ee4b98": {
      "type": "array",
      "description": "Arguments passed positionally to the script and exposed through the `arguments` object. Each item may be any JSON-serializable value (string, number, boolean, null, object, or array), matching what `executeScript` accepts.",
      "items": {},
      "default": []
    },
    "d464c9f79b398": {
      "type": "string",
      "description": "Content expected in the script's serialized return value. Doc Detective serializes non-string return values to JSON before matching. If the serialized return value doesn't contain the expected content, the step fails. Supports strings and regular expressions. To use a regular expression, the string must start and end with a forward slash, like in `/^hello-world.*/`."
    },
    "d52d050e4db1b": {
      "type": "string",
      "description": "Directory to save the script's return value. If the directory doesn't exist, creates the directory. If not specified, the directory is your media directory."
    },
    "d17ebe02c05e4": {
      "type": "number",
      "description": "Allowed variation as a fraction (0 to 1) of text different between the current return value and previously saved value. For example, 0.1 means 10%. If the difference between the current value and the previous value is greater than `maxVariation`, the step returns a warning. If no output exists at `path`, Doc Detective ignores this value.",
      "default": 0,
      "minimum": 0,
      "maximum": 1
    },
    "d67323486122f": {
      "type": "string",
      "description": "If `true`, overwrites the existing output at `path` if it exists.\nIf `aboveVariation`, overwrites the existing output at `path` if the difference between the new output and the existing output is greater than `maxVariation`.",
      "enum": [
        "true",
        "false",
        "aboveVariation"
      ],
      "default": "aboveVariation"
    },
    "dce8a8dcf91e3": {
      "type": "integer",
      "description": "Maximum time in milliseconds the script may run. If the script runs longer than this, the step fails.",
      "minimum": 1,
      "default": 60000
    },
    "d28cb52946bf2": {
      "surface": {
        "$ref": "#/x_dd_defs/d56daef404d20"
      },
      "script": {
        "$ref": "#/x_dd_defs/d99cdd6dfd51e"
      },
      "args": {
        "$ref": "#/x_dd_defs/d7a61b0ee4b98"
      },
      "output": {
        "$ref": "#/x_dd_defs/d464c9f79b398"
      },
      "path": {
        "type": "string",
        "description": "File path to save the script's serialized return value, relative to `directory`."
      },
      "directory": {
        "$ref": "#/x_dd_defs/d52d050e4db1b"
      },
      "maxVariation": {
        "$ref": "#/x_dd_defs/d17ebe02c05e4"
      },
      "overwrite": {
        "$ref": "#/x_dd_defs/d67323486122f"
      },
      "timeout": {
        "$ref": "#/x_dd_defs/dce8a8dcf91e3"
      }
    },
    "decc32cd319af": {
      "type": "object",
      "required": [
        "script"
      ],
      "additionalProperties": false,
      "properties": {
        "$ref": "#/x_dd_defs/d28cb52946bf2"
      },
      "title": "Run browser script (detailed)"
    }
  }
}