{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "waitUntil",
  "description": "Shared readiness conditions, shaped by the kind of surface a step targets. Browser surfaces wait on page conditions; process surfaces wait on stdio output or a fixed delay; app surfaces wait on an element or a fixed delay. Unlike goTo's navigation readiness, no condition applies by default — a step waits only on the conditions it names.",
  "anyOf": [
    {
      "title": "Browser readiness",
      "type": "object",
      "additionalProperties": false,
      "minProperties": 1,
      "properties": {
        "networkIdleTime": {
          "description": "Wait for network activity to be idle (no new requests) for this duration in milliseconds.",
          "type": "integer",
          "minimum": 0
        },
        "domIdleTime": {
          "description": "Wait for DOM mutations to stop for this duration in milliseconds.",
          "type": "integer",
          "minimum": 0
        },
        "find": {
          "title": "Element criteria",
          "type": "object",
          "description": "Wait for a specific element to be present. At least one finding field must be specified.",
          "additionalProperties": false,
          "anyOf": [
            {
              "required": [
                "selector"
              ]
            },
            {
              "required": [
                "elementText"
              ]
            },
            {
              "required": [
                "elementId"
              ]
            },
            {
              "required": [
                "elementTestId"
              ]
            },
            {
              "required": [
                "elementClass"
              ]
            },
            {
              "required": [
                "elementAttribute"
              ]
            },
            {
              "required": [
                "elementAria"
              ]
            },
            {
              "required": [
                "image"
              ]
            }
          ],
          "properties": {
            "selector": {
              "type": "string",
              "description": "Selector for the element to wait for. On browser surfaces, a CSS selector. On app surfaces, a native selector — an XPath (`//`), or `~` for an accessibility id; CSS is rejected at runtime."
            },
            "elementText": {
              "type": "string",
              "description": "Text content the element must contain. Supports exact match or regex pattern using /pattern/ syntax."
            },
            "elementId": {
              "type": "string",
              "description": "ID attribute of the element to find. Supports exact match or regex pattern using /pattern/ syntax."
            },
            "elementTestId": {
              "type": "string",
              "description": "data-testid attribute of the element to find. Supports exact match or regex pattern using /pattern/ syntax."
            },
            "elementClass": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                }
              ],
              "description": "Class or array of classes that the element must have. Each class supports exact match or regex pattern using /pattern/ syntax. Element must have all specified classes."
            },
            "elementAttribute": {
              "type": "object",
              "description": "Object of attribute key-value pairs that the element must have. Values can be strings (supporting /pattern/ regex), numbers, or booleans. Boolean true matches attribute presence, false matches absence.",
              "additionalProperties": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "number"
                  },
                  {
                    "type": "boolean"
                  }
                ]
              }
            },
            "elementAria": {
              "type": "string",
              "description": "Computed accessible name of the element per ARIA specification. Supports exact match or regex pattern using /pattern/ syntax."
            },
            "image": {
              "$schema": "http://json-schema.org/draft-07/schema#",
              "title": "image",
              "description": "Locate the element visually by template image (OpenCV template matching, auto-scaled across display scales). Combinable with other element criteria.",
              "$comment": "Keep tiny: this whole document inlines ~15,000x when dereferenced (ADR 01087). Region shape validates at runtime.",
              "anyOf": [
                {
                  "title": "Image (simple)",
                  "type": "string",
                  "minLength": 1,
                  "description": "Template image: a PNG/JPEG file path (resolved relative to the spec) or a data:image/…;base64 URI."
                },
                {
                  "title": "Image (detailed)",
                  "type": "object",
                  "additionalProperties": false,
                  "required": [
                    "path"
                  ],
                  "properties": {
                    "path": {
                      "type": "string",
                      "minLength": 1,
                      "description": "Template image: a PNG/JPEG file path (resolved relative to the spec) or a data:image/…;base64 URI."
                    },
                    "matchThreshold": {
                      "type": "number",
                      "minimum": 0,
                      "maximum": 1,
                      "description": "Minimum normalized match score (0–1). Defaults to the config-level `imageMatching.matchThreshold` (0.8)."
                    },
                    "region": {
                      "type": "object",
                      "minProperties": 1,
                      "description": "Search area: a rect ({x, y, width, height} in logical units) or element criteria (selector, elementText, …) whose match's bounds become the search area. Shape is validated at runtime; a nested `image` is rejected."
                    }
                  }
                }
              ],
              "components": {
                "schemas": {
                  "string": {
                    "title": "Image (simple)",
                    "type": "string",
                    "minLength": 1,
                    "description": "Template image: a PNG/JPEG file path (resolved relative to the spec) or a data:image/…;base64 URI."
                  },
                  "object": {
                    "title": "Image (detailed)",
                    "type": "object",
                    "additionalProperties": false,
                    "required": [
                      "path"
                    ],
                    "properties": {
                      "path": {
                        "type": "string",
                        "minLength": 1,
                        "description": "Template image: a PNG/JPEG file path (resolved relative to the spec) or a data:image/…;base64 URI."
                      },
                      "matchThreshold": {
                        "type": "number",
                        "minimum": 0,
                        "maximum": 1,
                        "description": "Minimum normalized match score (0–1). Defaults to the config-level `imageMatching.matchThreshold` (0.8)."
                      },
                      "region": {
                        "type": "object",
                        "minProperties": 1,
                        "description": "Search area: a rect ({x, y, width, height} in logical units) or element criteria (selector, elementText, …) whose match's bounds become the search area. Shape is validated at runtime; a nested `image` is rejected."
                      }
                    }
                  }
                }
              },
              "examples": [
                "icons/gear.png",
                {
                  "path": "icons/gear.png",
                  "matchThreshold": 0.9
                }
              ]
            }
          }
        }
      }
    },
    {
      "title": "Process readiness",
      "type": "object",
      "additionalProperties": false,
      "minProperties": 1,
      "properties": {
        "stdio": {
          "type": "string",
          "minLength": 1,
          "description": "Wait until combined stdout+stderr matches. Substring, or /regex/."
        },
        "delayMs": {
          "type": "integer",
          "minimum": 0,
          "description": "Fixed delay (ms)."
        }
      }
    },
    {
      "title": "App readiness",
      "type": "object",
      "additionalProperties": false,
      "minProperties": 1,
      "properties": {
        "delayMs": {
          "type": "integer",
          "minimum": 0,
          "description": "Fixed delay (ms)."
        },
        "find": {
          "description": "Wait for a specific element to exist on the app surface. Fields with no accessibility mapping on the target platform fail at runtime with the supported alternative named.",
          "title": "Element criteria",
          "type": "object",
          "additionalProperties": false,
          "anyOf": [
            {
              "required": [
                "selector"
              ]
            },
            {
              "required": [
                "elementText"
              ]
            },
            {
              "required": [
                "elementId"
              ]
            },
            {
              "required": [
                "elementTestId"
              ]
            },
            {
              "required": [
                "elementClass"
              ]
            },
            {
              "required": [
                "elementAttribute"
              ]
            },
            {
              "required": [
                "elementAria"
              ]
            },
            {
              "required": [
                "image"
              ]
            }
          ],
          "properties": {
            "selector": {
              "type": "string",
              "description": "Selector for the element to wait for. On browser surfaces, a CSS selector. On app surfaces, a native selector — an XPath (`//`), or `~` for an accessibility id; CSS is rejected at runtime."
            },
            "elementText": {
              "type": "string",
              "description": "Text content the element must contain. Supports exact match or regex pattern using /pattern/ syntax."
            },
            "elementId": {
              "type": "string",
              "description": "ID attribute of the element to find. Supports exact match or regex pattern using /pattern/ syntax."
            },
            "elementTestId": {
              "type": "string",
              "description": "data-testid attribute of the element to find. Supports exact match or regex pattern using /pattern/ syntax."
            },
            "elementClass": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "array",
                  "items": {
                    "type": "string"
                  }
                }
              ],
              "description": "Class or array of classes that the element must have. Each class supports exact match or regex pattern using /pattern/ syntax. Element must have all specified classes."
            },
            "elementAttribute": {
              "type": "object",
              "description": "Object of attribute key-value pairs that the element must have. Values can be strings (supporting /pattern/ regex), numbers, or booleans. Boolean true matches attribute presence, false matches absence.",
              "additionalProperties": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "number"
                  },
                  {
                    "type": "boolean"
                  }
                ]
              }
            },
            "elementAria": {
              "type": "string",
              "description": "Computed accessible name of the element per ARIA specification. Supports exact match or regex pattern using /pattern/ syntax."
            },
            "image": {
              "$schema": "http://json-schema.org/draft-07/schema#",
              "title": "image",
              "description": "Locate the element visually by template image (OpenCV template matching, auto-scaled across display scales). Combinable with other element criteria.",
              "$comment": "Keep tiny: this whole document inlines ~15,000x when dereferenced (ADR 01087). Region shape validates at runtime.",
              "anyOf": [
                {
                  "title": "Image (simple)",
                  "type": "string",
                  "minLength": 1,
                  "description": "Template image: a PNG/JPEG file path (resolved relative to the spec) or a data:image/…;base64 URI."
                },
                {
                  "title": "Image (detailed)",
                  "type": "object",
                  "additionalProperties": false,
                  "required": [
                    "path"
                  ],
                  "properties": {
                    "path": {
                      "type": "string",
                      "minLength": 1,
                      "description": "Template image: a PNG/JPEG file path (resolved relative to the spec) or a data:image/…;base64 URI."
                    },
                    "matchThreshold": {
                      "type": "number",
                      "minimum": 0,
                      "maximum": 1,
                      "description": "Minimum normalized match score (0–1). Defaults to the config-level `imageMatching.matchThreshold` (0.8)."
                    },
                    "region": {
                      "type": "object",
                      "minProperties": 1,
                      "description": "Search area: a rect ({x, y, width, height} in logical units) or element criteria (selector, elementText, …) whose match's bounds become the search area. Shape is validated at runtime; a nested `image` is rejected."
                    }
                  }
                }
              ],
              "components": {
                "schemas": {
                  "string": {
                    "title": "Image (simple)",
                    "type": "string",
                    "minLength": 1,
                    "description": "Template image: a PNG/JPEG file path (resolved relative to the spec) or a data:image/…;base64 URI."
                  },
                  "object": {
                    "title": "Image (detailed)",
                    "type": "object",
                    "additionalProperties": false,
                    "required": [
                      "path"
                    ],
                    "properties": {
                      "path": {
                        "type": "string",
                        "minLength": 1,
                        "description": "Template image: a PNG/JPEG file path (resolved relative to the spec) or a data:image/…;base64 URI."
                      },
                      "matchThreshold": {
                        "type": "number",
                        "minimum": 0,
                        "maximum": 1,
                        "description": "Minimum normalized match score (0–1). Defaults to the config-level `imageMatching.matchThreshold` (0.8)."
                      },
                      "region": {
                        "type": "object",
                        "minProperties": 1,
                        "description": "Search area: a rect ({x, y, width, height} in logical units) or element criteria (selector, elementText, …) whose match's bounds become the search area. Shape is validated at runtime; a nested `image` is rejected."
                      }
                    }
                  }
                }
              },
              "examples": [
                "icons/gear.png",
                {
                  "path": "icons/gear.png",
                  "matchThreshold": 0.9
                }
              ]
            }
          }
        }
      }
    }
  ],
  "components": {
    "schemas": {
      "browser": {
        "title": "Browser readiness",
        "type": "object",
        "additionalProperties": false,
        "minProperties": 1,
        "properties": {
          "networkIdleTime": {
            "description": "Wait for network activity to be idle (no new requests) for this duration in milliseconds.",
            "type": "integer",
            "minimum": 0
          },
          "domIdleTime": {
            "description": "Wait for DOM mutations to stop for this duration in milliseconds.",
            "type": "integer",
            "minimum": 0
          },
          "find": {
            "title": "Element criteria",
            "type": "object",
            "description": "Wait for a specific element to be present. At least one finding field must be specified.",
            "additionalProperties": false,
            "anyOf": [
              {
                "required": [
                  "selector"
                ]
              },
              {
                "required": [
                  "elementText"
                ]
              },
              {
                "required": [
                  "elementId"
                ]
              },
              {
                "required": [
                  "elementTestId"
                ]
              },
              {
                "required": [
                  "elementClass"
                ]
              },
              {
                "required": [
                  "elementAttribute"
                ]
              },
              {
                "required": [
                  "elementAria"
                ]
              },
              {
                "required": [
                  "image"
                ]
              }
            ],
            "properties": {
              "selector": {
                "type": "string",
                "description": "Selector for the element to wait for. On browser surfaces, a CSS selector. On app surfaces, a native selector — an XPath (`//`), or `~` for an accessibility id; CSS is rejected at runtime."
              },
              "elementText": {
                "type": "string",
                "description": "Text content the element must contain. Supports exact match or regex pattern using /pattern/ syntax."
              },
              "elementId": {
                "type": "string",
                "description": "ID attribute of the element to find. Supports exact match or regex pattern using /pattern/ syntax."
              },
              "elementTestId": {
                "type": "string",
                "description": "data-testid attribute of the element to find. Supports exact match or regex pattern using /pattern/ syntax."
              },
              "elementClass": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "array",
                    "items": {
                      "type": "string"
                    }
                  }
                ],
                "description": "Class or array of classes that the element must have. Each class supports exact match or regex pattern using /pattern/ syntax. Element must have all specified classes."
              },
              "elementAttribute": {
                "type": "object",
                "description": "Object of attribute key-value pairs that the element must have. Values can be strings (supporting /pattern/ regex), numbers, or booleans. Boolean true matches attribute presence, false matches absence.",
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    },
                    {
                      "type": "boolean"
                    }
                  ]
                }
              },
              "elementAria": {
                "type": "string",
                "description": "Computed accessible name of the element per ARIA specification. Supports exact match or regex pattern using /pattern/ syntax."
              },
              "image": {
                "$schema": "http://json-schema.org/draft-07/schema#",
                "title": "image",
                "description": "Locate the element visually by template image (OpenCV template matching, auto-scaled across display scales). Combinable with other element criteria.",
                "$comment": "Keep tiny: this whole document inlines ~15,000x when dereferenced (ADR 01087). Region shape validates at runtime.",
                "anyOf": [
                  {
                    "title": "Image (simple)",
                    "type": "string",
                    "minLength": 1,
                    "description": "Template image: a PNG/JPEG file path (resolved relative to the spec) or a data:image/…;base64 URI."
                  },
                  {
                    "title": "Image (detailed)",
                    "type": "object",
                    "additionalProperties": false,
                    "required": [
                      "path"
                    ],
                    "properties": {
                      "path": {
                        "type": "string",
                        "minLength": 1,
                        "description": "Template image: a PNG/JPEG file path (resolved relative to the spec) or a data:image/…;base64 URI."
                      },
                      "matchThreshold": {
                        "type": "number",
                        "minimum": 0,
                        "maximum": 1,
                        "description": "Minimum normalized match score (0–1). Defaults to the config-level `imageMatching.matchThreshold` (0.8)."
                      },
                      "region": {
                        "type": "object",
                        "minProperties": 1,
                        "description": "Search area: a rect ({x, y, width, height} in logical units) or element criteria (selector, elementText, …) whose match's bounds become the search area. Shape is validated at runtime; a nested `image` is rejected."
                      }
                    }
                  }
                ],
                "components": {
                  "schemas": {
                    "string": {
                      "title": "Image (simple)",
                      "type": "string",
                      "minLength": 1,
                      "description": "Template image: a PNG/JPEG file path (resolved relative to the spec) or a data:image/…;base64 URI."
                    },
                    "object": {
                      "title": "Image (detailed)",
                      "type": "object",
                      "additionalProperties": false,
                      "required": [
                        "path"
                      ],
                      "properties": {
                        "path": {
                          "type": "string",
                          "minLength": 1,
                          "description": "Template image: a PNG/JPEG file path (resolved relative to the spec) or a data:image/…;base64 URI."
                        },
                        "matchThreshold": {
                          "type": "number",
                          "minimum": 0,
                          "maximum": 1,
                          "description": "Minimum normalized match score (0–1). Defaults to the config-level `imageMatching.matchThreshold` (0.8)."
                        },
                        "region": {
                          "type": "object",
                          "minProperties": 1,
                          "description": "Search area: a rect ({x, y, width, height} in logical units) or element criteria (selector, elementText, …) whose match's bounds become the search area. Shape is validated at runtime; a nested `image` is rejected."
                        }
                      }
                    }
                  }
                },
                "examples": [
                  "icons/gear.png",
                  {
                    "path": "icons/gear.png",
                    "matchThreshold": 0.9
                  }
                ]
              }
            }
          }
        }
      },
      "findElement": {
        "title": "Element criteria",
        "type": "object",
        "description": "Wait for a specific element to be present. At least one finding field must be specified.",
        "additionalProperties": false,
        "anyOf": [
          {
            "required": [
              "selector"
            ]
          },
          {
            "required": [
              "elementText"
            ]
          },
          {
            "required": [
              "elementId"
            ]
          },
          {
            "required": [
              "elementTestId"
            ]
          },
          {
            "required": [
              "elementClass"
            ]
          },
          {
            "required": [
              "elementAttribute"
            ]
          },
          {
            "required": [
              "elementAria"
            ]
          },
          {
            "required": [
              "image"
            ]
          }
        ],
        "properties": {
          "selector": {
            "type": "string",
            "description": "Selector for the element to wait for. On browser surfaces, a CSS selector. On app surfaces, a native selector — an XPath (`//`), or `~` for an accessibility id; CSS is rejected at runtime."
          },
          "elementText": {
            "type": "string",
            "description": "Text content the element must contain. Supports exact match or regex pattern using /pattern/ syntax."
          },
          "elementId": {
            "type": "string",
            "description": "ID attribute of the element to find. Supports exact match or regex pattern using /pattern/ syntax."
          },
          "elementTestId": {
            "type": "string",
            "description": "data-testid attribute of the element to find. Supports exact match or regex pattern using /pattern/ syntax."
          },
          "elementClass": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "array",
                "items": {
                  "type": "string"
                }
              }
            ],
            "description": "Class or array of classes that the element must have. Each class supports exact match or regex pattern using /pattern/ syntax. Element must have all specified classes."
          },
          "elementAttribute": {
            "type": "object",
            "description": "Object of attribute key-value pairs that the element must have. Values can be strings (supporting /pattern/ regex), numbers, or booleans. Boolean true matches attribute presence, false matches absence.",
            "additionalProperties": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "number"
                },
                {
                  "type": "boolean"
                }
              ]
            }
          },
          "elementAria": {
            "type": "string",
            "description": "Computed accessible name of the element per ARIA specification. Supports exact match or regex pattern using /pattern/ syntax."
          },
          "image": {
            "$schema": "http://json-schema.org/draft-07/schema#",
            "title": "image",
            "description": "Locate the element visually by template image (OpenCV template matching, auto-scaled across display scales). Combinable with other element criteria.",
            "$comment": "Keep tiny: this whole document inlines ~15,000x when dereferenced (ADR 01087). Region shape validates at runtime.",
            "anyOf": [
              {
                "title": "Image (simple)",
                "type": "string",
                "minLength": 1,
                "description": "Template image: a PNG/JPEG file path (resolved relative to the spec) or a data:image/…;base64 URI."
              },
              {
                "title": "Image (detailed)",
                "type": "object",
                "additionalProperties": false,
                "required": [
                  "path"
                ],
                "properties": {
                  "path": {
                    "type": "string",
                    "minLength": 1,
                    "description": "Template image: a PNG/JPEG file path (resolved relative to the spec) or a data:image/…;base64 URI."
                  },
                  "matchThreshold": {
                    "type": "number",
                    "minimum": 0,
                    "maximum": 1,
                    "description": "Minimum normalized match score (0–1). Defaults to the config-level `imageMatching.matchThreshold` (0.8)."
                  },
                  "region": {
                    "type": "object",
                    "minProperties": 1,
                    "description": "Search area: a rect ({x, y, width, height} in logical units) or element criteria (selector, elementText, …) whose match's bounds become the search area. Shape is validated at runtime; a nested `image` is rejected."
                  }
                }
              }
            ],
            "components": {
              "schemas": {
                "string": {
                  "title": "Image (simple)",
                  "type": "string",
                  "minLength": 1,
                  "description": "Template image: a PNG/JPEG file path (resolved relative to the spec) or a data:image/…;base64 URI."
                },
                "object": {
                  "title": "Image (detailed)",
                  "type": "object",
                  "additionalProperties": false,
                  "required": [
                    "path"
                  ],
                  "properties": {
                    "path": {
                      "type": "string",
                      "minLength": 1,
                      "description": "Template image: a PNG/JPEG file path (resolved relative to the spec) or a data:image/…;base64 URI."
                    },
                    "matchThreshold": {
                      "type": "number",
                      "minimum": 0,
                      "maximum": 1,
                      "description": "Minimum normalized match score (0–1). Defaults to the config-level `imageMatching.matchThreshold` (0.8)."
                    },
                    "region": {
                      "type": "object",
                      "minProperties": 1,
                      "description": "Search area: a rect ({x, y, width, height} in logical units) or element criteria (selector, elementText, …) whose match's bounds become the search area. Shape is validated at runtime; a nested `image` is rejected."
                    }
                  }
                }
              }
            },
            "examples": [
              "icons/gear.png",
              {
                "path": "icons/gear.png",
                "matchThreshold": 0.9
              }
            ]
          }
        }
      },
      "app": {
        "title": "App readiness",
        "type": "object",
        "additionalProperties": false,
        "minProperties": 1,
        "properties": {
          "delayMs": {
            "type": "integer",
            "minimum": 0,
            "description": "Fixed delay (ms)."
          },
          "find": {
            "description": "Wait for a specific element to exist on the app surface. Fields with no accessibility mapping on the target platform fail at runtime with the supported alternative named.",
            "title": "Element criteria",
            "type": "object",
            "additionalProperties": false,
            "anyOf": [
              {
                "required": [
                  "selector"
                ]
              },
              {
                "required": [
                  "elementText"
                ]
              },
              {
                "required": [
                  "elementId"
                ]
              },
              {
                "required": [
                  "elementTestId"
                ]
              },
              {
                "required": [
                  "elementClass"
                ]
              },
              {
                "required": [
                  "elementAttribute"
                ]
              },
              {
                "required": [
                  "elementAria"
                ]
              },
              {
                "required": [
                  "image"
                ]
              }
            ],
            "properties": {
              "selector": {
                "type": "string",
                "description": "Selector for the element to wait for. On browser surfaces, a CSS selector. On app surfaces, a native selector — an XPath (`//`), or `~` for an accessibility id; CSS is rejected at runtime."
              },
              "elementText": {
                "type": "string",
                "description": "Text content the element must contain. Supports exact match or regex pattern using /pattern/ syntax."
              },
              "elementId": {
                "type": "string",
                "description": "ID attribute of the element to find. Supports exact match or regex pattern using /pattern/ syntax."
              },
              "elementTestId": {
                "type": "string",
                "description": "data-testid attribute of the element to find. Supports exact match or regex pattern using /pattern/ syntax."
              },
              "elementClass": {
                "anyOf": [
                  {
                    "type": "string"
                  },
                  {
                    "type": "array",
                    "items": {
                      "type": "string"
                    }
                  }
                ],
                "description": "Class or array of classes that the element must have. Each class supports exact match or regex pattern using /pattern/ syntax. Element must have all specified classes."
              },
              "elementAttribute": {
                "type": "object",
                "description": "Object of attribute key-value pairs that the element must have. Values can be strings (supporting /pattern/ regex), numbers, or booleans. Boolean true matches attribute presence, false matches absence.",
                "additionalProperties": {
                  "anyOf": [
                    {
                      "type": "string"
                    },
                    {
                      "type": "number"
                    },
                    {
                      "type": "boolean"
                    }
                  ]
                }
              },
              "elementAria": {
                "type": "string",
                "description": "Computed accessible name of the element per ARIA specification. Supports exact match or regex pattern using /pattern/ syntax."
              },
              "image": {
                "$schema": "http://json-schema.org/draft-07/schema#",
                "title": "image",
                "description": "Locate the element visually by template image (OpenCV template matching, auto-scaled across display scales). Combinable with other element criteria.",
                "$comment": "Keep tiny: this whole document inlines ~15,000x when dereferenced (ADR 01087). Region shape validates at runtime.",
                "anyOf": [
                  {
                    "title": "Image (simple)",
                    "type": "string",
                    "minLength": 1,
                    "description": "Template image: a PNG/JPEG file path (resolved relative to the spec) or a data:image/…;base64 URI."
                  },
                  {
                    "title": "Image (detailed)",
                    "type": "object",
                    "additionalProperties": false,
                    "required": [
                      "path"
                    ],
                    "properties": {
                      "path": {
                        "type": "string",
                        "minLength": 1,
                        "description": "Template image: a PNG/JPEG file path (resolved relative to the spec) or a data:image/…;base64 URI."
                      },
                      "matchThreshold": {
                        "type": "number",
                        "minimum": 0,
                        "maximum": 1,
                        "description": "Minimum normalized match score (0–1). Defaults to the config-level `imageMatching.matchThreshold` (0.8)."
                      },
                      "region": {
                        "type": "object",
                        "minProperties": 1,
                        "description": "Search area: a rect ({x, y, width, height} in logical units) or element criteria (selector, elementText, …) whose match's bounds become the search area. Shape is validated at runtime; a nested `image` is rejected."
                      }
                    }
                  }
                ],
                "components": {
                  "schemas": {
                    "string": {
                      "title": "Image (simple)",
                      "type": "string",
                      "minLength": 1,
                      "description": "Template image: a PNG/JPEG file path (resolved relative to the spec) or a data:image/…;base64 URI."
                    },
                    "object": {
                      "title": "Image (detailed)",
                      "type": "object",
                      "additionalProperties": false,
                      "required": [
                        "path"
                      ],
                      "properties": {
                        "path": {
                          "type": "string",
                          "minLength": 1,
                          "description": "Template image: a PNG/JPEG file path (resolved relative to the spec) or a data:image/…;base64 URI."
                        },
                        "matchThreshold": {
                          "type": "number",
                          "minimum": 0,
                          "maximum": 1,
                          "description": "Minimum normalized match score (0–1). Defaults to the config-level `imageMatching.matchThreshold` (0.8)."
                        },
                        "region": {
                          "type": "object",
                          "minProperties": 1,
                          "description": "Search area: a rect ({x, y, width, height} in logical units) or element criteria (selector, elementText, …) whose match's bounds become the search area. Shape is validated at runtime; a nested `image` is rejected."
                        }
                      }
                    }
                  }
                },
                "examples": [
                  "icons/gear.png",
                  {
                    "path": "icons/gear.png",
                    "matchThreshold": 0.9
                  }
                ]
              }
            }
          }
        }
      },
      "process": {
        "title": "Process readiness",
        "type": "object",
        "additionalProperties": false,
        "minProperties": 1,
        "properties": {
          "stdio": {
            "type": "string",
            "minLength": 1,
            "description": "Wait until combined stdout+stderr matches. Substring, or /regex/."
          },
          "delayMs": {
            "type": "integer",
            "minimum": 0,
            "description": "Fixed delay (ms)."
          }
        }
      }
    }
  },
  "examples": [
    {
      "stdio": "/^4$/"
    },
    {
      "delayMs": 500
    },
    {
      "networkIdleTime": 500,
      "domIdleTime": 1000
    },
    {
      "find": {
        "selector": ".ready"
      }
    }
  ]
}