{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "startSurface",
  "description": "Open (provision) one or more surfaces and register them by name so later steps can target them with `surface`. Three kinds: a native APP (Windows/macOS desktop by executable path, `.app` path, bundle ID, or UWP AppUserModelID; Android/iOS apps on managed emulators/simulators — macOS desktop additionally requires the Accessibility permission for the process that runs Doc Detective), a BROWSER session (opens blank and ready on the context's automation server; navigate it with a `goTo` step), or a background PROCESS (equivalent to `runShell` with `background` — both forms stay valid). An ARRAY of descriptors opens them all concurrently — the step completes when every one is ready, and device boots overlap. See docs/design/multi-surface-targeting.md and docs/design/native-app-surfaces.md.",
  "anyOf": [
    {
      "$ref": "#/x_dd_defs/de9d2967e5e93"
    },
    {
      "$ref": "#/x_dd_defs/d2b1b8896d1ef"
    },
    {
      "$ref": "#/x_dd_defs/d4f54163b58fb"
    },
    {
      "title": "Parallel surfaces",
      "type": "array",
      "minItems": 1,
      "items": {
        "anyOf": [
          {
            "$ref": "#/x_dd_defs/de9d2967e5e93"
          },
          {
            "$ref": "#/x_dd_defs/d2b1b8896d1ef"
          },
          {
            "$ref": "#/x_dd_defs/d4f54163b58fb"
          }
        ]
      },
      "description": "Open several surfaces concurrently (any mix of kinds). All descriptors launch in parallel; the step completes when every one is ready. Names must be unique within the array and across the context's open surfaces (checked at runtime). Device boots overlap — worth real wall-clock on 30–60s emulator starts."
    }
  ],
  "components": {
    "schemas": {
      "browserDescriptor": {
        "$ref": "#/x_dd_defs/d2b1b8896d1ef"
      },
      "processDescriptor": {
        "$ref": "#/x_dd_defs/d4f54163b58fb"
      },
      "processStartWaitUntil": {
        "$ref": "#/x_dd_defs/d7e7e734ea60a"
      },
      "appDescriptor": {
        "$ref": "#/x_dd_defs/de9d2967e5e93"
      },
      "deviceDescriptor": {
        "$ref": "#/x_dd_defs/d3108bb58115d"
      }
    }
  },
  "examples": [
    {
      "app": "C:\\Windows\\System32\\notepad.exe"
    },
    {
      "app": "Microsoft.WindowsCalculator_8wekyb3d8bbwe!App",
      "name": "calc"
    },
    {
      "app": "/Applications/Calculator.app",
      "name": "calc",
      "args": [
        "--reset"
      ],
      "waitUntil": {
        "delayMs": 500
      },
      "timeout": 30000
    },
    {
      "app": "com.example.myapp",
      "install": "./build/MyApp.apk",
      "device": {
        "platform": "android",
        "name": "Pixel_7"
      }
    },
    {
      "browser": "firefox",
      "name": "admin",
      "headless": true,
      "size": {
        "width": 1366,
        "height": 768
      }
    },
    {
      "process": "node server.js",
      "name": "api",
      "waitUntil": {
        "httpGet": "http://localhost:8080"
      },
      "timeout": 30000
    },
    [
      {
        "browser": "chrome",
        "name": "web"
      },
      {
        "process": "node server.js",
        "name": "api",
        "waitUntil": {
          "port": 8080
        }
      }
    ],
    [
      {
        "app": "com.example.chat",
        "name": "alice",
        "device": {
          "platform": "android",
          "name": "Pixel_7"
        }
      },
      {
        "app": "com.example.chat",
        "name": "bob",
        "device": {
          "platform": "android",
          "name": "Pixel_7_second"
        }
      }
    ]
  ],
  "x_dd_defs": {
    "d494f31569800": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "transform": [
        "trim"
      ],
      "description": "The app identifier: an executable path (`C:\\\\Windows\\\\System32\\\\notepad.exe`), a `.app` path, a bundle ID (`com.apple.TextEdit`), a package name (`com.example.myapp`), or a UWP AppUserModelID (`Microsoft.WindowsCalculator_8wekyb3d8bbwe!App`). Disambiguated by syntax — never by a type field."
    },
    "d00fd472eb300": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "transform": [
        "trim"
      ],
      "description": "Surface-registry name later steps use in `surface`. Default: the executable basename without extension, or the final dot-segment of an ID."
    },
    "d26f5b4ae44d5": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Launch arguments (desktop apps). On macOS they pass to the app as a real argument array. On Windows they join into a single shell-style argument string for the driver, so an argument with embedded spaces must carry its own quotes (e.g. \"\\\"My File.txt\\\"\")."
    },
    "d197e1397c016": {
      "type": "string",
      "minLength": 1,
      "description": "Working directory for the launched app. Not honored by the current desktop app drivers, so a non-default value fails with guidance: on Windows the NovaWindows driver ignores it (the app inherits the driver's own working directory), and on macOS the driver launches apps through LaunchServices, which offers no working-directory control. Launch the app via runShell if the cwd matters. Reserved for when a driver gains support. Default: the run's working directory.",
      "default": "."
    },
    "d1aad8af271f3": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "description": "Extra environment variables for the launched app (desktop apps). Supported on macOS; not supported by the Windows driver, where any value fails with guidance (set variables in the shell that launches Doc Detective instead)."
    },
    "dc4b036c9a00c": {
      "type": "string",
      "minLength": 1,
      "description": "Path to an installable artifact (`.apk`/`.app`/`.ipa`) to install on the device before launch. Supported on Android and iOS app surfaces."
    },
    "d981f1e264912": {
      "type": "string",
      "minLength": 1,
      "description": "Android main activity override (defaults to the package's launcher activity). Android-only."
    },
    "d404c8af71621": {
      "type": "string",
      "enum": [
        "android",
        "ios"
      ],
      "description": "Target platform. Selects the mobile driver. Required in `startSurface.device`; implied by the context in `context.device`."
    },
    "dd187ef483549": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "transform": [
        "trim"
      ],
      "description": "Device name and registry identity — the same name resolves to the same device. Reference form: names an existing AVD (Android) / simulator (iOS) to reuse. If no device by this name exists, Doc Detective creates one under this name using `deviceType`/`osVersion` (or their defaults), provided the toolchain is installed (`doc-detective install android` or `doc-detective install ios`)."
    },
    "d91c6cc8594df": {
      "type": "string",
      "enum": [
        "phone",
        "tablet"
      ],
      "description": "Abstract hardware profile used when creating a device (portable across `android`/`ios`). Doc Detective maps it to a built-in profile. Ignored when `name` already matches an existing device. Default: `phone`."
    },
    "d76bf42938bc8": {
      "type": "string",
      "minLength": 1,
      "description": "Platform version used when creating a device; must match an installed image/runtime for the target platform (install more with `doc-detective install android` or `doc-detective install ios`). Ignored when `name` already matches an existing device. Default: the newest installed version."
    },
    "dad62b593ec79": {
      "type": "boolean",
      "description": "Run the Android emulator without a window. No-op on iOS (simulators boot without the Simulator UI on CI) and ignored where not applicable."
    },
    "db78778cee370": {
      "type": "string",
      "enum": [
        "portrait",
        "landscape"
      ],
      "description": "Initial orientation. Reserved; validated now, not yet implemented."
    },
    "d26dc4b5b604e": {
      "type": "string",
      "minLength": 1,
      "description": "Pin a specific device/emulator instance by UDID. Reserved; validated now, not yet implemented."
    },
    "d97688f05ec3a": {
      "type": "object",
      "description": "Cloud device farm configuration, keyed by provider. Reserved; validated now, not yet implemented."
    },
    "dcee12847a79f": {
      "platform": {
        "$ref": "#/x_dd_defs/d404c8af71621"
      },
      "name": {
        "$ref": "#/x_dd_defs/dd187ef483549"
      },
      "deviceType": {
        "$ref": "#/x_dd_defs/d91c6cc8594df"
      },
      "osVersion": {
        "$ref": "#/x_dd_defs/d76bf42938bc8"
      },
      "headless": {
        "$ref": "#/x_dd_defs/dad62b593ec79"
      },
      "orientation": {
        "$ref": "#/x_dd_defs/db78778cee370"
      },
      "udid": {
        "$ref": "#/x_dd_defs/d26dc4b5b604e"
      },
      "provider": {
        "$ref": "#/x_dd_defs/d97688f05ec3a"
      }
    },
    "d3108bb58115d": {
      "title": "Device descriptor",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "$ref": "#/x_dd_defs/dcee12847a79f"
      }
    },
    "d772bd37ae789": {
      "allOf": [
        {
          "$ref": "#/x_dd_defs/d3108bb58115d"
        },
        {
          "required": [
            "platform"
          ]
        }
      ]
    },
    "dd456181e5531": {
      "description": "Device the app runs on. Omit for a host desktop app, or (in a mobile context) to use the context's default device. A string references a device by name; an object refines it. Supported on Android and iOS mobile targets.",
      "anyOf": [
        {
          "type": "string",
          "minLength": 1,
          "pattern": "\\S",
          "transform": [
            "trim"
          ],
          "title": "Device (by name)"
        },
        {
          "$ref": "#/x_dd_defs/d772bd37ae789"
        }
      ]
    },
    "df6b433caff2b": {
      "type": "object",
      "description": "Escape-hatch passthrough: merged into the automation session's capabilities after the ones Doc Detective computes (namespaced per driver, e.g. `appium:noReset`). Driver- and version-specific; use sparingly."
    },
    "deacb609dfcf7": {
      "type": "string",
      "description": "Selector for the element to wait for. On browser surfaces, a CSS selector. On app surfaces, a native selector — an XPath (`//`), or `~` for an accessibility id; CSS is rejected at runtime."
    },
    "d85ea5a8fe519": {
      "type": "string",
      "description": "Text content the element must contain. Supports exact match or regex pattern using /pattern/ syntax."
    },
    "d51f552441335": {
      "type": "string",
      "description": "ID attribute of the element to find. Supports exact match or regex pattern using /pattern/ syntax."
    },
    "de98afd642d66": {
      "type": "string",
      "description": "data-testid attribute of the element to find. Supports exact match or regex pattern using /pattern/ syntax."
    },
    "d36879a346bf0": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "array",
          "items": {
            "type": "string"
          }
        }
      ],
      "description": "Class or array of classes that the element must have. Each class supports exact match or regex pattern using /pattern/ syntax. Element must have all specified classes."
    },
    "df5fb044d9398": {
      "type": "object",
      "description": "Object of attribute key-value pairs that the element must have. Values can be strings (supporting /pattern/ regex), numbers, or booleans. Boolean true matches attribute presence, false matches absence.",
      "additionalProperties": {
        "anyOf": [
          {
            "type": "string"
          },
          {
            "type": "number"
          },
          {
            "type": "boolean"
          }
        ]
      }
    },
    "da993bab30b8c": {
      "type": "string",
      "description": "Computed accessible name of the element per ARIA specification. Supports exact match or regex pattern using /pattern/ syntax."
    },
    "df337647aaf18": {
      "selector": {
        "$ref": "#/x_dd_defs/deacb609dfcf7"
      },
      "elementText": {
        "$ref": "#/x_dd_defs/d85ea5a8fe519"
      },
      "elementId": {
        "$ref": "#/x_dd_defs/d51f552441335"
      },
      "elementTestId": {
        "$ref": "#/x_dd_defs/de98afd642d66"
      },
      "elementClass": {
        "$ref": "#/x_dd_defs/d36879a346bf0"
      },
      "elementAttribute": {
        "$ref": "#/x_dd_defs/df5fb044d9398"
      },
      "elementAria": {
        "$ref": "#/x_dd_defs/da993bab30b8c"
      }
    },
    "d66e2d47c68a2": {
      "description": "Wait for a specific element to exist on the app surface. Fields with no accessibility mapping on the target platform fail at runtime with the supported alternative named.",
      "title": "Element criteria",
      "type": "object",
      "additionalProperties": false,
      "anyOf": [
        {
          "required": [
            "selector"
          ]
        },
        {
          "required": [
            "elementText"
          ]
        },
        {
          "required": [
            "elementId"
          ]
        },
        {
          "required": [
            "elementTestId"
          ]
        },
        {
          "required": [
            "elementClass"
          ]
        },
        {
          "required": [
            "elementAttribute"
          ]
        },
        {
          "required": [
            "elementAria"
          ]
        }
      ],
      "properties": {
        "$ref": "#/x_dd_defs/df337647aaf18"
      }
    },
    "d46cd8fc75284": {
      "delayMs": {
        "type": "integer",
        "minimum": 0,
        "description": "Fixed delay (ms)."
      },
      "find": {
        "$ref": "#/x_dd_defs/d66e2d47c68a2"
      }
    },
    "d4b1c3b536583": {
      "description": "Startup readiness: a fixed delay and/or an element that must exist before the surface is considered open. No condition applies by default.",
      "title": "App readiness",
      "type": "object",
      "additionalProperties": false,
      "minProperties": 1,
      "properties": {
        "$ref": "#/x_dd_defs/d46cd8fc75284"
      }
    },
    "d91366291613f": {
      "type": "integer",
      "minimum": 0,
      "description": "Startup ceiling in milliseconds (launch + install + readiness).",
      "default": 60000
    },
    "dda87819d852d": {
      "app": {
        "$ref": "#/x_dd_defs/d494f31569800"
      },
      "name": {
        "$ref": "#/x_dd_defs/d00fd472eb300"
      },
      "args": {
        "$ref": "#/x_dd_defs/d26f5b4ae44d5"
      },
      "workingDirectory": {
        "$ref": "#/x_dd_defs/d197e1397c016"
      },
      "env": {
        "$ref": "#/x_dd_defs/d1aad8af271f3"
      },
      "install": {
        "$ref": "#/x_dd_defs/dc4b036c9a00c"
      },
      "activity": {
        "$ref": "#/x_dd_defs/d981f1e264912"
      },
      "device": {
        "$ref": "#/x_dd_defs/dd456181e5531"
      },
      "driverOptions": {
        "$ref": "#/x_dd_defs/df6b433caff2b"
      },
      "waitUntil": {
        "$ref": "#/x_dd_defs/d4b1c3b536583"
      },
      "timeout": {
        "$ref": "#/x_dd_defs/d91366291613f"
      }
    },
    "de9d2967e5e93": {
      "title": "App descriptor",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "app"
      ],
      "properties": {
        "$ref": "#/x_dd_defs/dda87819d852d"
      }
    },
    "df1433a25f706": {
      "type": "string",
      "enum": [
        "chrome",
        "firefox",
        "safari",
        "webkit",
        "edge"
      ],
      "description": "Browser engine to open. The session opens on the context's automation server with a blank page and registers as a surface; use a goTo step (with `surface`) to navigate it. No cross-engine fallback — the one exception is `edge`, which is Chromium and opens on the same Chrome/chromedriver stack (it registers under the `chrome` engine, and defaults its surface name to `chrome` when you don't set one)."
    },
    "d4fa80fde0b8b": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "transform": [
        "trim"
      ],
      "description": "Surface-registry name later steps use in `surface`. Default: the engine name. Must be unique across all open surfaces — the context's default browser already owns its engine name, so name a second same-engine session explicitly. An engine keyword may only name a session of that engine."
    },
    "d6f5b5742ae9f": {
      "width": {
        "type": "integer",
        "description": "Outer window width in pixels."
      },
      "height": {
        "type": "integer",
        "description": "Outer window height in pixels."
      }
    },
    "db4398501f15f": {
      "title": "Browser window size",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "$ref": "#/x_dd_defs/d6f5b5742ae9f"
      },
      "description": "Outer window dimensions for this session. Default: the context's browser window size."
    },
    "dd1b59b285631": {
      "width": {
        "type": "integer",
        "description": "Viewport width in pixels."
      },
      "height": {
        "type": "integer",
        "description": "Viewport height in pixels."
      }
    },
    "d568830bce6b9": {
      "title": "Browser viewport size",
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "$ref": "#/x_dd_defs/dd1b59b285631"
      },
      "description": "Viewport (page-content) dimensions for this session; the window is resized so the content area matches. Takes precedence over `size` when both are set."
    },
    "d476a3efe523d": {
      "type": "object",
      "description": "Escape-hatch passthrough: merged into the session's capabilities after the ones Doc Detective computes. Driver- and version-specific; use sparingly."
    },
    "d43d68e626ed5": {
      "browser": {
        "$ref": "#/x_dd_defs/df1433a25f706"
      },
      "name": {
        "$ref": "#/x_dd_defs/d4fa80fde0b8b"
      },
      "headless": {
        "type": "boolean",
        "description": "Run this session headless. Default: the context's browser headless setting."
      },
      "size": {
        "$ref": "#/x_dd_defs/db4398501f15f"
      },
      "viewport": {
        "$ref": "#/x_dd_defs/d568830bce6b9"
      },
      "driverOptions": {
        "$ref": "#/x_dd_defs/d476a3efe523d"
      }
    },
    "d2b1b8896d1ef": {
      "title": "Browser descriptor",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "browser"
      ],
      "properties": {
        "$ref": "#/x_dd_defs/d43d68e626ed5"
      }
    },
    "da98a7b6fe70d": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "transform": [
        "trim"
      ],
      "description": "Shell command to start as a long-running background process (same shell semantics as runShell: pipes, `&&`, globbing, environment-variable expansion). Equivalent to a `runShell` step with `background` — both forms remain valid."
    },
    "d9d72702a63fc": {
      "type": "string",
      "minLength": 1,
      "pattern": "\\S",
      "transform": [
        "trim"
      ],
      "description": "Surface-registry name later steps use in `surface`, and the handle a closeSurface step stops. Unique across all open surfaces."
    },
    "d516fb6492d61": {
      "type": "boolean",
      "default": false,
      "description": "Run the process in a pseudo-terminal (PTY) instead of a pipe, so full-screen/interactive TUIs (those that check `isTTY`) render and accept keystrokes. Requires the PTY backend `@homebridge/node-pty-prebuilt-multiarch` to be installed (`npm install @homebridge/node-pty-prebuilt-multiarch`); it is not bundled, and if it is unavailable the descriptor is skipped. `stdout` and `stderr` are merged into one stream in PTY mode. PTY output includes raw ANSI escape sequences (colors, cursor movement); `waitUntil.stdio` patterns should target text that renders without interleaved control codes, or use a regex that tolerates them."
    },
    "d5516989a70cb": {
      "type": "integer",
      "description": "Wait until this TCP port accepts connections on localhost.",
      "minimum": 1,
      "maximum": 65535
    },
    "d406ef25357d6": {
      "type": "string",
      "description": "Wait until the process's output contains this content. Searches both stdout and stderr. Supports strings and regular expressions. To use a regular expression, the string must start and end with a forward slash, like in `/ready on \\d+/`.",
      "minLength": 1
    },
    "ddddaa6f9ad11": {
      "port": {
        "$ref": "#/x_dd_defs/d5516989a70cb"
      },
      "stdio": {
        "$ref": "#/x_dd_defs/d406ef25357d6"
      },
      "httpGet": {
        "type": "string",
        "description": "Wait until an HTTP GET request to this URL returns a 2xx status."
      },
      "delayMs": {
        "type": "integer",
        "description": "Wait at least this many milliseconds.",
        "minimum": 0
      }
    },
    "d7e7e734ea60a": {
      "$comment": "Intentionally identical to runShell_v3's background.waitUntil — both are executed by the shared waitForReady. NOT a $ref to waitUntil_v3's process shape: that shape (stdio/delayMs only) is consumed by `type`, and extending it would leak conditions `type` can't execute.",
      "type": "object",
      "description": "Conditions that must all be met before the process is considered ready and the descriptor completes. Omit to consider the process ready as soon as it is spawned. Specify any combination; every condition given must pass before `timeout` elapses. Note: a process that forks a daemon and then exits (common for some Docker images and databases) is treated as having exited before becoming ready and the descriptor fails — use `port`, `httpGet`, or `delayMs` for those rather than a condition that depends on the foreground process staying alive.",
      "additionalProperties": false,
      "minProperties": 1,
      "properties": {
        "$ref": "#/x_dd_defs/ddddaa6f9ad11"
      }
    },
    "dcc9d1e4e36e2": {
      "type": "integer",
      "minimum": 0,
      "default": 60000,
      "description": "Max time in milliseconds to wait for `waitUntil` before the descriptor fails."
    },
    "def802e2f72ac": {
      "process": {
        "$ref": "#/x_dd_defs/da98a7b6fe70d"
      },
      "name": {
        "$ref": "#/x_dd_defs/d9d72702a63fc"
      },
      "args": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "default": [],
        "description": "Arguments for the command."
      },
      "workingDirectory": {
        "type": "string",
        "default": ".",
        "description": "Working directory for the process."
      },
      "tty": {
        "$ref": "#/x_dd_defs/d516fb6492d61"
      },
      "waitUntil": {
        "$ref": "#/x_dd_defs/d7e7e734ea60a"
      },
      "timeout": {
        "$ref": "#/x_dd_defs/dcc9d1e4e36e2"
      }
    },
    "d4f54163b58fb": {
      "title": "Process descriptor",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "process",
        "name"
      ],
      "properties": {
        "$ref": "#/x_dd_defs/def802e2f72ac"
      }
    }
  }
}