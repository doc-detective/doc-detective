{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "closeSurface",
  "description": "Close one or more surfaces: background processes, browser windows/tabs or whole browser sessions, and native app surfaces. A browser reference with a `tab` selector closes that tab; with a `window` selector it closes the window and its tabs; with neither it closes the whole browser session. An app reference ({ \"app\": … }) closes the app surface, terminating the app when Doc Detective launched it. Closing a surface that is not open is a no-op (PASS). Renames `stopProcess`.",
  "anyOf": [
    {
      "$ref": "surface_v3.schema.json#"
    },
    {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "surface_v3.schema.json#"
      }
    }
  ],
  "examples": [
    "web",
    {
      "process": "web"
    },
    [
      "web",
      "api"
    ],
    {
      "browser": "chrome",
      "tab": "cart"
    },
    {
      "browser": "chrome",
      "window": "admin"
    },
    {
      "browser": "chrome",
      "tab": -1
    },
    "chrome",
    {
      "app": "charmap"
    },
    {
      "app": "notepad",
      "window": -1
    },
    {
      "app": "TextEdit",
      "window": {
        "title": "Untitled 2"
      }
    }
  ]
}
