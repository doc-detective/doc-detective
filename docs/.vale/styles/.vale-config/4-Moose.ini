# Moose: the house voice for hawkeyexl repos. Direct only; no other voice.
#
# The Voices core and the Direct voice are vendored into this package from
# jdkato/voices (see NOTICE) with a handful of rule fixes, so there is no
# nested package to fetch and nothing to pin. Refresh them deliberately from
# upstream, re-apply the fixes, and cut a new release.
MinAlertLevel = suggestion

[*.{md,mdx,txt}]
BasedOnStyles = Voices, Direct, Moose
