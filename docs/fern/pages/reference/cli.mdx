---
title: CLI commands and flags
description: "Reference for the doc-detective command line: commands and every flag."
---

<Note>
This page is a placeholder created during the documentation information-architecture overhaul. Full content is planned — see the content set in `docs/content-strategy/information-architecture.md`.
</Note>

Reference for `doc-detective` commands (default run, `install`, `debug`) and all flags. Authoritative source: `buildYargs()` in `src/utils.ts`.
