---
title: "Selector node"
description: "Reference for the `Selector node` schema."
---

{/* Generated by docs/.scripts/buildSchemaReferencesV4.js. Do not edit by hand;
     update the schema in src/common and run `npm run docs:build-schema-refs`. */}

## Fields

Field | Type | Description | Default
:-- | :-- | :-- | :--
comment | object([Content filter selector](/reference/schemas/content-filter-selector)) | Optional. Match comment nodes (non-rendered author notes). | —
codeBlock | one of:<br/>- string<br/>- object([Code block selector](/reference/schemas/code-block-selector)) | Optional. Match fenced/source code blocks. A string is shorthand for `{ "language": "<value>" }`. | —
link | object([Link selector (chained)](/reference/schemas/link-selector-chained)) | Optional. Match hyperlink nodes. | —
image | object([Image selector (chained)](/reference/schemas/image-selector-chained)) | Optional. Match image nodes. | —
strong | object([Strong/emphasis selector (chained)](/reference/schemas/strong-emphasis-selector-chained)) | Optional. Match bold/strong text nodes. | —
emphasis | object([Strong/emphasis selector (chained)](/reference/schemas/strong-emphasis-selector-chained)) | Optional. Match italic/emphasis text nodes. | —
text | object([Content filter selector](/reference/schemas/content-filter-selector)) | Optional. Match prose text runs (never inside code blocks or comments). | —
element | one of:<br/>- string<br/>- object([Element selector (chained)](/reference/schemas/element-selector-chained)) | Optional. Match a named element (HTML/XML tag or MDX JSX component). A string is shorthand for `{ "tag": "<value>" }`. | —

## Examples

```json
{
  "comment": {
    "matches": "example",
    "excludes": "example"
  },
  "codeBlock": "example",
  "strong": {
    "text": "example"
  },
  "emphasis": {
    "text": "example"
  },
  "text": {
    "matches": "example",
    "excludes": "example"
  },
  "element": "example"
}
```
