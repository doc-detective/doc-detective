---
title: "Selector node"
description: "Reference for the `Selector node` schema."
---

{/* Generated by docs/.scripts/buildSchemaReferencesV4.js. Do not edit by hand;
     update the schema in src/common and run `npm run docs:build-schema-refs`. */}

## Fields

Field | Type | Description | Default
:-- | :-- | :-- | :--
comment | object([Content filter selector](/reference/schemas/content-filter-selector)) | Optional. Match a node by its content — a comment's body or a text run's prose. Referenced by the `comment` and `text` selector kinds and by statement containers. | —
codeBlock | one of:<br/>- string<br/>- object([Code block selector](/reference/schemas/code-block-selector)) | Optional. Match fenced/source code blocks. A string is shorthand for `{ "language": "<value>" }`. | —
link | object([Link selector (chained)](/reference/schemas/link-selector-chained)) | Optional. Match a hyperlink node. Used for `followedBy.then` chaining and statement containers. | —
image | object([Image selector (chained)](/reference/schemas/image-selector-chained)) | Optional. Match an image node. Used for `followedBy.then` chaining and statement containers. | —
strong | object([Strong/emphasis selector (chained)](/reference/schemas/strong-emphasis-selector-chained)) | Optional. Match a bold/strong or italic/emphasis text node. Used for `followedBy.then` chaining and statement containers. | —
emphasis | object([Strong/emphasis selector (chained)](/reference/schemas/strong-emphasis-selector-chained)) | Optional. Match a bold/strong or italic/emphasis text node. Used for `followedBy.then` chaining and statement containers. | —
text | object([Content filter selector](/reference/schemas/content-filter-selector)) | Optional. Match a node by its content — a comment's body or a text run's prose. Referenced by the `comment` and `text` selector kinds and by statement containers. | —
element | one of:<br/>- string<br/>- object([Element selector (chained)](/reference/schemas/element-selector-chained)) | Optional. Match a named element (HTML/XML tag or MDX JSX component). A string is shorthand for `{ "tag": "<value>" }`. | —

## Examples

```json
{
  "element": {
    "tag": "uicontrol"
  }
}
```
