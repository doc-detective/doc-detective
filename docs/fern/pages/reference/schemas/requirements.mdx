---
title: "Requirements"
description: "Reference for the `Requirements` schema."
---

{/* Generated by docs/.scripts/buildSchemaReferencesV4.js. Do not edit by hand;
     update the schema in src/common and run `npm run docs:build-schema-refs`. */}

## Referenced In

- [context](/reference/schemas/context)

## Fields

Field | Type | Description | Default
:-- | :-- | :-- | :--
commands | array of string | Optional. Commands that must be resolvable on the PATH. | —
files | array of string | Optional. Files that must exist. Entries support `$VAR` and `$HOME` expansion. | —
env | array of string | Optional. Environment variables that must be set to a non-empty value. | —

## Examples

```json
{
  "commands": [
    "node",
    "ffmpeg"
  ]
}
```

```json
{
  "commands": [
    "adb"
  ],
  "files": [
    "$HOME/.config/app.toml"
  ],
  "env": [
    "API_TOKEN"
  ]
}
```
