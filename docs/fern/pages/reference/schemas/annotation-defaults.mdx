---
title: "Annotation defaults"
description: "Default visual theme for screenshot and recording annotations. Specs and tests can override this value with their own `annotationDefaults` fields (test level wins over spec level, which wins over config level), and an individual annotation's `style` wins over all of them. Fields left unset fall back to Doc Detective's built-in theme. Setting a theme here keeps every annotated image in a doc set visually consistent."
---

{/* Generated by docs/.scripts/buildSchemaReferencesV4.js. Do not edit by hand;
     update the schema in src/common and run `npm run docs:build-schema-refs`. */}

Default visual theme for screenshot and recording annotations. Specs and tests can override this value with their own `annotationDefaults` fields (test level wins over spec level, which wins over config level), and an individual annotation's `style` wins over all of them. Fields left unset fall back to Doc Detective's built-in theme. Setting a theme here keeps every annotated image in a doc set visually consistent.

## Referenced In

- [config](/reference/schemas/config)
- [specification](/reference/schemas/specification)
- [test](/reference/schemas/test)

## Fields

Field | Type | Description | Default
:-- | :-- | :-- | :--
color | string | Optional. Default foreground color for every annotation type. | —
background | string | Optional. Default background color for text-bearing annotations. | —
strokeWidth | number | Optional. Default line width in pixels.<br/><br/>Minimum: 0 | —
fontSize | number | Optional. Default font size in pixels.<br/><br/>Minimum: 1 | —
fontFamily | string | Optional. Default font family. | —
opacity | number | Optional. Default opacity, from 0 to 1.<br/><br/>Minimum: 0. Maximum: 1 | —
radius | number | Optional. Default corner radius in pixels.<br/><br/>Minimum: 0 | —
padding | number | Optional. Default padding in pixels.<br/><br/>Minimum: 0 | —
maxWidth | number | Optional. Default maximum text width in pixels.<br/><br/>Minimum: 1 | —
intensity | number | Optional. Default blur strength.<br/><br/>Minimum: 1 | —
outline | object([Annotation style](/reference/schemas/annotation-style)) | Optional. Style overrides applied to every `outline` annotation. | —
arrow | object([Annotation style](/reference/schemas/annotation-style)) | Optional. Style overrides applied to every `arrow` annotation. | —
badge | object([Annotation style](/reference/schemas/annotation-style)) | Optional. Style overrides applied to every `badge` annotation. | —
callout | object([Annotation style](/reference/schemas/annotation-style)) | Optional. Style overrides applied to every `callout` annotation. | —
blur | object([Annotation style](/reference/schemas/annotation-style)) | Optional. Style overrides applied to every `blur` annotation. | —
text | object([Annotation style](/reference/schemas/annotation-style)) | Optional. Style overrides applied to every `text` annotation. | —
transition | object([Annotation transition](/reference/schemas/annotation-transition)) | Optional. Default transition for annotations that don't set their own. | —

## Examples

```json
{
  "color": "#E11D48",
  "strokeWidth": 3,
  "badge": {
    "background": "#E11D48",
    "color": "#FFFFFF"
  },
  "blur": {
    "intensity": 14
  }
}
```

```json
{
  "fontFamily": "Inter, system-ui, sans-serif",
  "fontSize": 14,
  "transition": {
    "enter": "fade",
    "exit": "fade",
    "durationMs": 250
  }
}
```
