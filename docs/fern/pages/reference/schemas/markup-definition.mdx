---
title: "Markup definition"
description: "Reference for the `Markup definition` schema."
---

{/* Generated by docs/.scripts/buildSchemaReferencesV4.js. Do not edit by hand;
     update the schema in src/common and run `npm run docs:build-schema-refs`. */}

## Referenced In

- [File type (custom)](/reference/schemas/file-type-custom)

## Fields

Field | Type | Description | Default
:-- | :-- | :-- | :--
name | string | Optional. Name of the markup definition | —
regex | one of:<br/>- string<br/>- array of string | Optional. Regular expressions to match the markup type. | —
comment | object([Content filter selector](/reference/schemas/content-filter-selector)) | Optional. Match comment nodes (non-rendered author notes). | —
codeBlock | one of:<br/>- string<br/>- object([Code block selector](/reference/schemas/code-block-selector)) | Optional. Match fenced/source code blocks. A string is shorthand for `{ "language": "<value>" }`. | —
link | object([Link selector](/reference/schemas/link-selector)) | Optional. Match hyperlink nodes. | —
image | object([Image selector](/reference/schemas/image-selector)) | Optional. Match image nodes. | —
strong | object([Strong/emphasis selector](/reference/schemas/strong-emphasis-selector)) | Optional. Match bold/strong text nodes. | —
emphasis | object([Strong/emphasis selector](/reference/schemas/strong-emphasis-selector)) | Optional. Match italic/emphasis text nodes. | —
text | object([Content filter selector](/reference/schemas/content-filter-selector)) | Optional. Match prose text runs (never inside code blocks or comments). | —
element | one of:<br/>- string<br/>- object([Element selector](/reference/schemas/element-selector)) | Optional. Match a named element (HTML/XML tag or MDX JSX component). A string is shorthand for `{ "tag": "<value>" }`. | —
captures | array of string | Optional. Selector mode only. Node field paths mapped in order to $1..$n for action substitution. Supported paths per kind: `content`, `text`, `language`, `meta`, `url`, `title`, `src`, `alt`, `tag`, `attributes.<name>`, `match.<n>` (decimal group index), and `then.<field>`. If omitted, the kind's documented default capture order applies. | —
batchMatches | boolean | Optional. If `true`, all matches are combined into a single string. | `false`
actions | one of:<br/>- string<br/>- array of one of: string, object | Optional. Actions to perform when the markup type is detected. | —

## Examples

```json
{
  "name": "example",
  "regex": "example",
  "comment": {
    "matches": "example",
    "excludes": "example"
  },
  "codeBlock": "example",
  "text": {
    "matches": "example",
    "excludes": "example"
  },
  "element": "example",
  "captures": [],
  "batchMatches": false,
  "actions": "annotate"
}
```
