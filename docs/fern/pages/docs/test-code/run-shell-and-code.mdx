---
title: Run shell and code steps
description: Execute commands and scripts and assert on their output.
---

<Note>
This page is a placeholder created during the documentation information-architecture overhaul. Full content is planned — see the content set in `docs/content-strategy/information-architecture.md`.
</Note>

Use `runShell` for native commands, `runCode` for inline code, and `runBrowserScript` for JavaScript in the browser. See the [runShell](/docs/actions/runshell), [runCode](/docs/actions/runcode), and [runBrowserScript](/docs/actions/runbrowserscript) action references.
