.husky/** text eol=lf

# Schema reference pages are emitted with LF by docs/.scripts/buildSchemaReferencesV4.js.
# Pin them to LF so the schema-refs drift check is stable across platforms.
docs/fern/pages/reference/schemas/*.md text eol=lf
